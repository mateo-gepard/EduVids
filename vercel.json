{
	"version": 2,
	"installCommand": "npm ci --ignore-scripts",
	"buildCommand": "npm run build:client",
	"builds": [
		{
			"src": "api/[...path].js",
			"use": "@vercel/node"
		},
		{
			"src": "dist/client/**",
			"use": "@vercel/static"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "api/[...path].js"
		},
		{
			"handle": "filesystem"
		},
		{
			"src": "/(.*)",
			"dest": "/index.html"
		}
	]
}
